#!/bin/bash

set -e

dir="$( cd "$(dirname "${BASH_SOURCE[0]}")" ; pwd -P )"

export ZIG_GLOBAL_CACHE_DIR="$dir../../gitignore/zig/cache"
export ZIG_LOCAL_CACHE_DIR="$dir../../gitignore/zig/cache"

zig c++ -target x86_64-windows "$@"