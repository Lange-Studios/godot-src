[project]
name = "godot-src"
version = "0.1.0"
description = "Working with godot from source made easy!"
authors = ["Lange Studios"]
channels = ["conda-forge"]
platforms = ["linux-64"]

[tasks]

[dependencies]
nushell = ">=0.95.0,<0.96"
dotnet = ">=8.0.302,<8.1"
